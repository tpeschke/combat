import { PanicThresholdsPipe } from './panic-thresholds.pipe';

describe('PanicThresholdsPipe', () => {
  it('create an instance', () => {
    const pipe = new PanicThresholdsPipe();
    expect(pipe).toBeTruthy();
  });
});
