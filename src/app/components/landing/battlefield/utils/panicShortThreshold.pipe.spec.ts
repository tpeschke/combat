/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { PanicShortThresholdPipe } from './panicShortThreshold.pipe';

describe('Pipe: PanicShortThresholde', () => {
  it('create an instance', () => {
    let pipe = new PanicShortThresholdPipe();
    expect(pipe).toBeTruthy();
  });
});
